<Window x:Class="PL.Engineer.EngineerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Engineer"
        mc:Ignorable="d"
        Title="EngineerListWindow" Height="640" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}" Foreground="#00000000" Background="#FFEFB9A4">
    <!--Extrnal Grid-->
    <Grid x:Name="loa">
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--Internal, Upper Grid-->
        <Grid Height="auto" Grid.Row="0" Width="auto" Margin="10,3,0,536" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="14*"/>
                <RowDefinition Height="71*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="21*"/>
                <ColumnDefinition Width="374*"/>
                <ColumnDefinition Width="388*"/>
                <ColumnDefinition Width="7*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Label" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Grid.Column="0" Grid.RowSpan="2" Grid.ColumnSpan="2"/>
            <ComboBox SelectedValue="{Binding Path=EngineerExperience, Mode=TwoWay}" ItemsSource="{Binding Source={StaticResource EngineerExperiencesCollectionKey}}" Grid.Column="2" SelectionChanged="cmbEngineerExperience_SelectionChange" Grid.ColumnSpan="2" Grid.RowSpan="2"  />

        </Grid>

        <ListView x:Name="load" Margin="5,47,5,148" Grid.Row="1" ItemsSource="{Binding EngineerList}" MouseDoubleClick="lsvDisplayEngineers_MouseDoubleClick">
            <ListView.View >
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Add" Grid.Row="1" HorizontalAlignment="Center" Width="100"  Margin="0,469,0,0" Height="59" VerticalAlignment="Top" Click="btnAddEngineer_Click"/>


    </Grid>
</Window>
